version: '3'

services:
  portfolio:
    hostname: portfolio
    container_name: portfolio
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 8765:8765 
    environment:
      - PORT=8765
